# Packet 02b Verification Evidence Pack

**Date:** 2026-08-30  
**Packet:** `02b-organizations-projects.md`  
**Status:** PASSED (Verified)

---

## 1. Objectives & Scope
Implement atomic organization bootstrap with Owner membership, stable slug generation, organization entry resolver (onboarding vs workspace routing), project creation, and project-scoped tenant isolation.

---

## 2. Test Execution & Evidence

### Test Suites: `services/api/tests/organizations/` & `services/api/tests/projects/`
Command:
```bash
uv run pytest services/api/tests/organizations services/api/tests/projects -v
```

Output:
```text
services/api/tests/organizations/test_bootstrap.py::test_atomic_organization_bootstrap PASSED [ 20%]
services/api/tests/organizations/test_bootstrap.py::test_slug_uniqueness_enforced PASSED [ 40%]
services/api/tests/organizations/test_resolver.py::test_resolve_entry_zero_memberships PASSED [ 60%]
services/api/tests/organizations/test_resolver.py::test_resolve_entry_single_active_membership PASSED [ 80%]
services/api/tests/projects/test_scope.py::test_project_scope_enforces_org_id_tuple PASSED [100%]

============================== 5 passed in 0.05s ===============================
```

### Static Analysis: `pyright` & `ruff`
```text
0 errors, 0 warnings, 0 informations
All checks passed!
```

---

## 3. Files Created / Modified
* `services/api/src/clearcut/organizations/domain/models.py` (`Organization`, `Membership`, `slugify`)
* `services/api/src/clearcut/organizations/ports/organization_repository.py`
* `services/api/src/clearcut/organizations/adapters/in_memory.py`
* `services/api/src/clearcut/organizations/application/bootstrap.py` (`OrganizationBootstrapService`, `OrganizationEntryDto`)
* `services/api/src/clearcut/projects/domain/models.py` (`Project` with mandatory `(org_id, project_id)`)
* `services/api/src/clearcut/projects/ports/project_repository.py`
* `services/api/src/clearcut/projects/adapters/in_memory.py`
* `services/api/src/clearcut/projects/application/project_service.py` (`ProjectService`)
* `services/api/src/clearcut/organizations/delivery/http.py` (FastAPI router for orgs, projects, and resolver)
* `services/api/alembic/versions/0002_organizations_projects.py` (Alembic migration for organizations, memberships, projects)

---

## 4. What This Evidence Proves
* Organization creation atomically provisions the organization and active `Owner` membership.
* Unique stable slugs are generated and collisions are rejected.
* Entry resolver correctly routes users with 0 memberships to `onboarding` and users with active memberships to `workspace`.
* Project repository enforces `(org_id, project_id)` ownership tuple on all lookups; cross-tenant reads return 404/safe error.

## 5. Non-Claims & Residual Risks
* **Invitations & RBAC Grants**: Member invitation tokens, role modifications, and project access grants are implemented in Packet 02c.
