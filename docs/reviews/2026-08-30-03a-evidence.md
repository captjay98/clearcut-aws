# Packet 03a Verification Evidence Pack

**Date:** 2026-08-30  
**Packet:** `03a-upload-artifacts.md`  
**Status:** PASSED (Verified)

---

## 1. Objectives & Scope
Implement one-time upload capabilities bound to org/project/actor with nonce and expiry, server-side byte verification (SHA-256 calculation and magic bytes validation for Fountain, FDX, and PDF formats), replay attack prevention, cross-project access rejection, and Alembic migration `0004_import_artifacts.py`.

---

## 2. Test Execution & Evidence

### Test Suite: `services/api/tests/scripts/`
Command:
```bash
uv run pytest services/api/tests/scripts/ -v
```

Output:
```text
services/api/tests/scripts/test_upload_capabilities.py::test_upload_capability_and_finalization PASSED [ 25%]
services/api/tests/scripts/test_upload_capabilities.py::test_magic_bytes_validation PASSED [ 50%]
services/api/tests/scripts/test_upload_security.py::test_replay_attack_rejected PASSED [ 75%]
services/api/tests/scripts/test_upload_security.py::test_cross_project_finalization_rejected PASSED [100%]

============================== 4 passed in 0.05s ===============================
```

### Static Analysis: `pyright` & `ruff`
```text
0 errors, 0 warnings, 0 informations
All checks passed!
```

---

## 3. Files Created / Modified
* `services/api/src/clearcut/scripts/domain/artifacts.py` (`UploadCapability`, `ImportArtifact`, `validate_screenplay_magic_bytes`, `compute_sha256`)
* `services/api/src/clearcut/scripts/ports/object_storage.py` (`ObjectStoragePort`)
* `services/api/src/clearcut/scripts/adapters/in_memory_storage.py` (`InMemoryObjectStorage`)
* `services/api/src/clearcut/scripts/application/upload_service.py` (`UploadService` with 25MB limit and replay prevention)
* `services/api/src/clearcut/scripts/delivery/http.py` (FastAPI router for capabilities and artifact finalization)
* `services/api/alembic/versions/0004_import_artifacts.py` (Alembic migration for `import_artifacts`)
* `services/api/tests/scripts/test_upload_capabilities.py`
* `services/api/tests/scripts/test_upload_security.py`

---

## 4. What This Evidence Proves
* Upload capabilities are single-use with cryptographic nonces and expiration timestamps.
* Server calculates SHA-256 checksum and verifies magic bytes directly from raw bytes, preventing file extension spoofing.
* Replaying used capabilities or attempting cross-project finalization is blocked.

## 5. Non-Claims & Residual Risks
* **Parsing & Span Tagging**: Screenplay parsing into immutable elements/spans is implemented in Packet 03b.
