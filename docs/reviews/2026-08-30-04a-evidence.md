# Packet 04a Verification Evidence Pack

**Date:** 2026-08-30  
**Packet:** `04a-gemini-detection.md`  
**Status:** PASSED (Verified)

---

## 1. Objectives & Scope
Implement the ten protected clearance categories (`real_persons_living`, `real_persons_deceased`, `corporate_entities`, `products_and_trademarks`, `copyrighted_works`, `music_and_lyrics`, `locations_and_landmarks`, `vehicles_and_insignia`, `sensitive_historical_events`, `contact_information`), candidate detection contracts, hermetic detection runtime, durable operations `Job` domain, and Alembic migration `0006_detection_jobs.py`.

---

## 2. Test Execution & Evidence

### Test Suites: `services/api/tests/detection/` & `services/api/tests/conformance/`
Command:
```bash
uv run pytest services/api/tests/detection services/api/tests/conformance/test_model_runtime.py -v
```

Output:
```text
services/api/tests/conformance/test_model_runtime.py::test_runtime_returns_typed_candidates PASSED [ 25%]
services/api/tests/detection/test_categories.py::test_ten_protected_categories_exist PASSED [ 50%]
services/api/tests/detection/test_categories.py::test_candidate_detection_across_categories PASSED [ 75%]
services/api/tests/detection/test_detection_jobs.py::test_detection_job_execution_and_idempotency PASSED [100%]

============================== 4 passed in 0.05s ===============================
```

### Static Analysis: `pyright` & `ruff`
```text
0 errors, 0 warnings, 0 informations
All checks passed!
```

---

## 3. Files Created / Modified
* `services/api/src/clearcut/detection/domain/candidates.py` (`ClearanceCategory` 10-enum, `CandidateItem`, `ClearanceItem`)
* `services/api/src/clearcut/detection/ports/model_runtime.py` (`ModelRuntimePort`)
* `services/api/src/clearcut/detection/adapters/hermetic_runtime.py` (`HermeticDetectionRuntime` covering all 10 categories)
* `services/api/src/clearcut/detection/application/detect.py` (`DetectionService`)
* `services/api/src/clearcut/operations/domain/jobs.py` (`Job`, `RunStatus`)
* `services/api/alembic/versions/0006_detection_jobs.py` (Alembic migration for `jobs` and `clearance_items`)

---

## 4. What This Evidence Proves
* All ten protected clearance categories are strictly modeled and typed.
* Detection runtime produces candidates with character span boundaries, category tags, rationale, and uncertainty metrics.
* Jobs support idempotent execution and lease management without fabricating downstream clearance evidence.

## 5. Non-Claims & Residual Risks
* **Evaluation & Scoring**: Deterministic gates and 10-dimension rubric evaluation are implemented in Packet 04b.
