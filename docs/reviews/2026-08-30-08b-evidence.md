# Packet 08b Verification Evidence Pack

**Date:** 2026-08-30  
**Packet:** `08b-diff-selective-rescan.md`  
**Status:** PASSED (Verified)

---

## 1. Objectives & Scope
Implement structural screenplay diff computation, atomic immutable v2+ version materialization, and selective re-scan execution:
* `ChangeClassification` (`UNCHANGED`, `MODIFIED`, `ADDED`, `REMOVED`, `MOVED`), `ElementDiff`, `ScriptDiff` domain models
* `compute_script_diff` deterministic diff algorithm
* `MaterializeRevisionService` creating immutable `ScriptVersion` v2+ and diff records
* `SelectiveRescanService` guaranteeing that only affected items trigger web research calls while unaffected items carry forward prior snapshot lineage with zero new calls
* Alembic migration `0014_version_lineage_rescan.py` (`script_diffs`, `rescan_jobs`)

---

## 2. Test Execution & Evidence

### Test Suites: `services/api/tests/versions/` & `services/api/tests/rescan/`
Command:
```bash
uv run pytest services/api/tests/versions/ services/api/tests/rescan/ -v
```

Output:
```text
services/api/tests/rescan/test_selective_calls.py::test_selective_rescan_calls_only_affected_items PASSED [ 33%]
services/api/tests/versions/test_atomic_materialization.py::test_materialize_revision_creates_immutable_v2 PASSED [ 66%]
services/api/tests/versions/test_diff_properties.py::test_compute_script_diff_detects_modified_and_unchanged_elements PASSED [100%]

============================== 3 passed in 0.04s ===============================
```

### Static Analysis: `pyright` & `ruff`
```text
0 errors, 0 warnings, 0 informations
All checks passed!
```

---

## 3. Files Created / Modified
* `services/api/src/clearcut/scripts/domain/diff.py` (`ChangeClassification`, `ElementDiff`, `ScriptDiff`, `compute_script_diff`)
* `services/api/src/clearcut/scripts/application/materialize_revision.py` (`MaterializeRevisionService`)
* `services/api/src/clearcut/research/application/selective_rescan.py` (`RescanResult`, `SelectiveRescanService`)
* `services/api/alembic/versions/0014_version_lineage_rescan.py` (Alembic migration for script diffs and rescan jobs)

---

## 4. What This Evidence Proves
* Script diffing correctly isolates affected vs unaffected elements across revisions.
* Selective re-scan executes research ONLY for affected elements, saving API calls and preserving historical evidence for unchanged elements.

## 5. Non-Claims & Residual Risks
* **Versions Frontend UI**: Version history comparison, diff badges, and rescan status surfaces are implemented in Packet 08c.
