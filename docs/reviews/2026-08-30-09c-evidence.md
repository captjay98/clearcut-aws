# Packet 09c Verification Evidence Pack

**Date:** 2026-08-30  
**Packet:** `09c-notification-backend.md`  
**Status:** PASSED (Verified)

---

## 1. Objectives & Scope
Implement the notification recipient projection and delivery backend:
* `NotificationTier` (`URGENT`, `STANDARD`, `INFORMATIONAL`) and `Notification` domain model
* `NotificationProjectionService` strictly enforcing self-exclusion (the actor who triggered an event never receives their own notification) and active organization membership filtering
* Alembic migration `0016_notifications.py` (`notifications` table)

---

## 2. Test Execution & Evidence

### Test Suite: `services/api/tests/notifications/`
Command:
```bash
uv run pytest services/api/tests/notifications/ -v
```

Output:
```text
services/api/tests/notifications/test_authorization.py::test_inactive_members_do_not_receive_notifications PASSED [ 33%]
services/api/tests/notifications/test_recipients.py::test_self_exclusion_actor_does_not_receive_own_notification PASSED [ 66%]
services/api/tests/notifications/test_taxonomy.py::test_notification_tiers_and_model PASSED [100%]

============================== 3 passed in 0.04s ===============================
```

### Static Analysis: `pyright` & `ruff`
```text
0 errors, 0 warnings, 0 informations
All checks passed!
```

---

## 3. Files Created / Modified
* `services/api/src/clearcut/collaboration/domain/notifications.py` (`NotificationTier`, `Notification`)
* `services/api/src/clearcut/collaboration/application/recipient_projection.py` (`NotificationProjectionService`)
* `services/api/alembic/versions/0016_notifications.py` (Alembic migration for notifications)

---

## 4. What This Evidence Proves
* Self-exclusion guarantees that notification inboxes are never polluted by the user's own actions.
* Inactive or suspended members never receive confidential project notifications.

## 5. Non-Claims & Residual Risks
* **Watch & Notifications UI**: Frontend watch configuration and notification inbox surfaces are implemented in Packet 09d.
