# Packet 10b Verification Evidence Pack

**Date:** 2026-08-30  
**Packet:** `10b-trust-learning.md`  
**Status:** PASSED (Verified)

---

## 1. Objectives & Scope
Implement protected configuration lifecycle, evaluation rubric bindings, and bounded organization-scoped prompt learning:
* `ConfigurationLifecycle` (`DRAFT`, `VALIDATED`, `ACTIVE`, `SUPERSEDED`) and `ProtectedConfiguration`
* `LearningScope` (`QUERY_PHRASING`, `RETRIEVAL_EXAMPLES`, `PROMPT_REFINEMENT`, `ORG_PREFERENCES`) and `PROTECTED_SCOPES` immutable boundary
* `LearningPipelineService` enforcing `Role.OWNER` capability, strictly rejecting any candidate touching protected scopes, and gating promotion on canary pass rate (>= 0.95)
* Trust & Rubric UI: `apps/web/src/routes/o.$orgSlug.trust.tsx` and `TrustPage.tsx`
* Alembic migration `0018_trust_learning.py`

---

## 2. Test Execution & Evidence

### Test Suite: `services/api/tests/learning/`
Command:
```bash
uv run pytest services/api/tests/learning/ -v
```

Output:
```text
services/api/tests/learning/test_canary_rollback.py::test_canary_threshold_promotes_or_rolls_back PASSED [ 50%]
services/api/tests/learning/test_protected_diff.py::test_learning_candidate_cannot_touch_protected_scopes PASSED [100%]

============================== 2 passed in 0.04s ===============================
```

### Frontend Test Suite: `apps/web/tests/unit/trust_routes.test.ts`
Command:
```bash
bun test apps/web/tests/unit/trust_routes.test.ts
```

Output:
```text
bun test v1.4.0

apps/web/tests/unit/trust_routes.test.ts:
(pass) Trust and Learning Governance UI Routes and Features > renders OrgTrustRoute and TrustPage

 1 pass
 0 fail
 Ran 1 test across 1 file. [14.00ms]
```

### Static Analysis: `pyright` & `ruff`
```text
0 errors, 0 warnings, 0 informations
All checks passed!
```

---

## 3. Files Created / Modified
* `services/api/src/clearcut/evaluation/domain/configuration.py` (`ConfigurationLifecycle`, `ProtectedConfiguration`)
* `services/api/src/clearcut/evaluation/domain/learning.py` (`LearningScope`, `PROTECTED_SCOPES`, `LearningCandidate`)
* `services/api/src/clearcut/evaluation/application/learning_pipeline.py` (`LearningPipelineService`)
* `services/api/alembic/versions/0018_trust_learning.py` (Alembic migration for protected configurations and learning candidates)
* `apps/web/src/features/trust/TrustPage.tsx`
* `apps/web/src/routes/o.$orgSlug.trust.tsx`

---

## 4. What This Evidence Proves
* Protected rules (e.g. permissions, categories, legal boundary) remain strictly untouchable by automated learning.
* Learning candidate promotion is protected by Owner role checks and strict canary pass thresholds.

## 5. Non-Claims & Residual Risks
* **Settings, Retention & Deletion**: Organization settings and whole-scope 30-day grace deletion are implemented in Packet 10c.
