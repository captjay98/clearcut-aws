# Packet 10c Verification Evidence Pack

**Date:** 2026-08-30  
**Packet:** `10c-settings-deletion.md`  
**Status:** PASSED (Verified)

---

## 1. Objectives & Scope
Implement organization settings, core evidence retention policy (no automated age deletion), and recoverable 30-day grace deletion lifecycle:
* `DeletionState` (`ACTIVE`, `GRACE_PERIOD`, `PURGED`), `OrganizationDeletionSchedule`, and `DeletionTombstone` domain models
* `DeletionService` enforcing `Role.OWNER` capability for scheduling and restoring deletion
* Permanent purge recording explicit tombstone and reproducibility loss
* Settings UI: `apps/web/src/routes/o.$orgSlug.settings.tsx` and `SettingsPage.tsx`
* Alembic migration `0019_deletion.py`

---

## 2. Test Execution & Evidence

### Test Suite: `services/api/tests/deletion/`
Command:
```bash
uv run pytest services/api/tests/deletion/ -v
```

Output:
```text
services/api/tests/deletion/test_reproducibility_loss.py::test_purge_creates_tombstone_and_records_reproducibility_loss PASSED [ 33%]
services/api/tests/deletion/test_schedule_restore_purge.py::test_schedule_and_restore_deletion_lifecycle PASSED [ 66%]
services/api/tests/deletion/test_schedule_restore_purge.py::test_non_owner_cannot_schedule_deletion PASSED [100%]

============================== 3 passed in 0.04s ===============================
```

### Frontend Test Suite: `apps/web/tests/unit/settings_routes.test.ts`
Command:
```bash
bun test apps/web/tests/unit/settings_routes.test.ts
```

Output:
```text
bun test v1.4.0

apps/web/tests/unit/settings_routes.test.ts:
(pass) Settings and Retention UI Routes and Features > renders OrgSettingsRoute and SettingsPage

 1 pass
 0 fail
 Ran 1 test across 1 file. [15.00ms]
```

### Static Analysis: `pyright` & `ruff`
```text
0 errors, 0 warnings, 0 informations
All checks passed!
```

---

## 3. Files Created / Modified
* `services/api/src/clearcut/audit/domain/deletion.py` (`DeletionState`, `OrganizationDeletionSchedule`, `DeletionTombstone`)
* `services/api/src/clearcut/audit/application/deletion_service.py` (`DeletionService`)
* `services/api/alembic/versions/0019_deletion.py` (Alembic migration for organization deletion schedules and tombstones)
* `apps/web/src/features/settings/SettingsPage.tsx`
* `apps/web/src/routes/o.$orgSlug.settings.tsx`

---

## 4. What This Evidence Proves
* Organization deletion has a mandatory 30-day grace period during which an Owner can restore full access.
* Purged organizations leave an immutable tombstone explicitly stating reproducibility loss without rewriting past audit events.

## 5. Non-Claims & Residual Risks
* **Production Dossier Packaging & Sealed Distribution**: Export packaging, PDF/HTML generation, and release verification are implemented in Plan 11.
