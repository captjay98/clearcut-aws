# Packet 11b Verification Evidence Pack

**Date:** 2026-08-30  
**Packet:** `11b-report-release-export.md`  
**Status:** PASSED (Verified)

---

## 1. Objectives & Scope
Implement governed report release attestation, deterministic HTML dossier rendering, and release audit events:
* `ReportRelease` domain model
* `ReleaseReportService` enforcing distinct human release attestation and role authorization (`Role.OWNER`, `Role.ADMIN`, `Role.REVIEWER`)
* `DeterministicReportRenderer` rendering immutable HTML dossiers with frozen script version bindings, content hashes, and explicit legal boundary disclaimers
* Alembic migration `0021_report_releases.py` (`report_releases` table)

---

## 2. Test Execution & Evidence

### Test Suite: `services/api/tests/reports/`
Command:
```bash
uv run pytest services/api/tests/reports/test_release.py services/api/tests/reports/test_renderer_determinism.py -v
```

Output:
```text
services/api/tests/reports/test_release.py::test_release_snapshot_commits_release_and_audit PASSED [ 50%]
services/api/tests/reports/test_renderer_determinism.py::test_render_html_is_deterministic_and_contains_legal_boundary PASSED [100%]

============================== 2 passed in 0.04s ===============================
```

### Static Analysis: `pyright` & `ruff`
```text
0 errors, 0 warnings, 0 informations
All checks passed!
```

---

## 3. Files Created / Modified
* `services/api/src/clearcut/export/domain/releases.py` (`ReportRelease`)
* `services/api/src/clearcut/export/application/release_report.py` (`ReleaseReportService`)
* `services/api/src/clearcut/export/adapters/html_pdf_renderer.py` (`DeterministicReportRenderer`)
* `services/api/alembic/versions/0021_report_releases.py` (Alembic migration for report releases)

---

## 4. What This Evidence Proves
* Release and generation are strictly separate lifecycle events with distinct audit records.
* HTML dossier rendering is byte-deterministic and includes the mandatory pre-clearance disclaimer.

## 5. Non-Claims & Residual Risks
* **Report UI & Print Isolation**: Frontend preview, release dialogs, and print stylesheets are implemented in Packet 11c.
